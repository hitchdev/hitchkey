# HitchKey Bootstrapper

The bootstrapper is a small, self contained golang executable
designed to run on linux, Mac OS X and Windows.

It will set up an isolated project environment for running
hitchkey commands.
