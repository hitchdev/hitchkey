- name: If virtualenv is not found, say so
  scenario:
    - Run:
       cmd: sudo apt-get install python-setuptools python3 -y
       timeout: 240
    - Run:
       cmd: sudo apt-get install python-pip -y
       timeout: 480
    - Run: cd /hitchkey ; sudo pip install .
    - Run:
       cmd: cd /hitchkey/tests/simple ; h command
       expect: You must have virtualenv
